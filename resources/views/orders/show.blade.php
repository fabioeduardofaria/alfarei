@extends('layouts.app', ['title' => $order->number.' · Alfarei CNC'])
@section('content')
<div class="heading"><div><p class="eyebrow">PEDIDO · {{ $order->number }}</p><h1>{{ $order->customer->name }}</h1><p class="muted">Origem: orçamento {{ $order->quote?->number ?? '—' }} · criado em {{ $order->created_at->format('d/m/Y H:i') }}</p></div><a class="secondary" href="{{ route('pedidos.index') }}">← Pedidos</a></div>
<section class="order-layout"><div><article class="panel order-journey"><div class="panel-head"><div><h2>Jornada do pedido</h2><p>A produção só é liberada depois da entrada e da arte, quando aplicável.</p></div><span class="status {{ $order->status }}">{{ ['awaiting_deposit' => 'Aguardando entrada', 'awaiting_art' => 'Aguardando arte', 'ready_for_production' => 'Liberado para produção', 'in_production' => 'Em produção', 'quality' => 'Conferência', 'ready' => 'Pronto', 'delivered' => 'Entregue', 'cancelled' => 'Cancelado'][$order->status] }}</span></div><ol class="order-steps"><li class="done"><i>✓</i><span>Pedido criado<small>{{ $order->created_at->format('d/m H:i') }}</small></span></li><li class="{{ $order->deposit_paid_at ? 'done' : ($order->status === 'awaiting_deposit' ? 'current' : '') }}"><i>{{ $order->deposit_paid_at ? '✓' : '2' }}</i><span>Entrada<small>{{ $order->deposit_paid_at ? 'Confirmada' : 'Pendente' }}</small></span></li><li class="{{ $order->art_approved_at ? 'done' : ($order->status === 'awaiting_art' ? 'current' : '') }}"><i>{{ $order->art_approved_at ? '✓' : '3' }}</i><span>Aprovação de arte<small>{{ $order->art_approved_at ? 'Aprovada' : 'Quando aplicável' }}</small></span></li><li class="{{ $order->status === 'ready_for_production' ? 'current' : '' }}"><i>4</i><span>Produção<small>Aguardando OP</small></span></li><li><i>5</i><span>Conferência<small>Aguardando</small></span></li><li><i>6</i><span>Entrega<small>Aguardando</small></span></li></ol></article><article class="panel table-panel order-items"><div class="panel-head"><div><h2>Itens do pedido</h2><p>Valores e custos congelados na conversão do orçamento.</p></div></div><table><thead><tr><th>ITEM</th><th>QTD.</th><th>CUSTO</th><th>TOTAL</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td><b>{{ $item->description }}</b><small>{{ $item->made_to_order ? 'Sob encomenda' : 'Estoque' }}</small></td><td>{{ number_format($item->quantity, 3, ',', '.') }}</td><td>R$ {{ number_format($item->total_cost, 2, ',', '.') }}</td><td><b>R$ {{ number_format($item->total, 2, ',', '.') }}</b></td></tr>@endforeach</tbody></table></article></div><aside class="panel order-actions"><p class="eyebrow">PRÓXIMA AÇÃO</p>@if($order->status === 'awaiting_deposit')<h2>Confirmar entrada</h2><p>Registre o recebimento de 50% para avançar o pedido.</p><strong>R$ {{ number_format($order->deposit_amount, 2, ',', '.') }}</strong><form method="POST" action="{{ route('pedidos.confirm-deposit', $order) }}">@csrf<button class="primary full">Confirmar recebimento →</button></form>@elseif($order->status === 'awaiting_art')<h2>Aprovar arte</h2><p>Confirme o aceite da prévia pelo cliente antes de iniciar a produção.</p><form method="POST" action="{{ route('pedidos.approve-art', $order) }}">@csrf<button class="primary full">Registrar aprovação →</button></form>@elseif($order->status === 'ready_for_production')<h2>Pronto para produção</h2><p>Entrada e arte confirmadas. A próxima etapa criará a Ordem de Produção.</p><span class="status ready_for_production">Liberado</span>@else<h2>Pedido em andamento</h2><p>Este pedido está seguindo o fluxo operacional.</p>@endif<div class="order-values"><span>Total do pedido <b>R$ {{ number_format($order->total, 2, ',', '.') }}</b></span><span>Custo estimado <b>R$ {{ number_format($order->cost_total, 2, ',', '.') }}</b></span><span>Margem <b>{{ $order->total > 0 ? number_format((($order->total - $order->cost_total) / $order->total) * 100, 1, ',', '.') : '0,0' }}%</b></span></div></aside></section>
@endsection
