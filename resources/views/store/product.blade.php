@extends('store.layout')
@section('content')
<section class="product-page"><div class="product-hero" @if($product->image_url) style="background-image:url('{{ $product->image_url }}')" @endif><span>{{ $product->made_to_order ? 'Produzido sob encomenda' : 'Pronta entrega' }}</span><b>{{ strtoupper(substr($product->name, 0, 1)) }}</b></div><div class="product-info"><a class="back-store" href="{{ route('loja.index') }}">← Voltar à loja</a><p class="store-eyebrow">{{ $product->type === 'service' ? 'SERVIÇO' : 'PRODUTO ALFAREI' }}</p><h1>{{ $product->name }}</h1><p class="product-description">{{ $product->description }}</p><strong class="product-price">R$ {{ number_format($product->base_price, 2, ',', '.') }}</strong><form method="POST" action="{{ route('loja.add', $product) }}">@csrf@if($product->allow_personalization)<label>Como deseja personalizar?<textarea name="personalization" placeholder="Ex.: nome, frase, medidas, acabamento ou referência visual."></textarea></label>@endif<label>Quantidade<select name="quantity"><option value="1">1 unidade</option><option value="2">2 unidades</option><option value="3">3 unidades</option><option value="5">5 unidades</option></select></label><button class="store-primary" type="submit">Adicionar ao carrinho →</button></form><small class="delivery-note">{{ $settings->delivery_message }}</small></div></section>
@endsection
