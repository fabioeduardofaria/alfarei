<article class="store-product"><a href="{{ route('loja.product', $product) }}" class="product-visual" aria-label="Ver {{ $product->name }}" @if($product->image_url) style="background-image:url('{{ $product->image_url }}')" @endif><span>{{ $product->allow_personalization ? 'Personalizável' : 'Produção Alfarei' }}</span>@unless($product->image_url)<b>{{ strtoupper(substr($product->name, 0, 1)) }}</b>@endunless</a><div><p>{{ $product->made_to_order ? 'Sob encomenda' : 'Disponível' }}</p><h3>{{ $product->name }}</h3><small>{{ Str::limit($product->description, 82) }}</small><strong>R$ {{ number_format($product->base_price, 2, ',', '.') }}</strong><a href="{{ route('loja.product', $product) }}">Conhecer a peça <span aria-hidden="true">→</span></a></div></article>
