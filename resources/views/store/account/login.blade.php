@extends('store.layout')

@section('content')
<section class="store-account-page"><div class="store-account-intro"><p class="store-eyebrow">SUA CONTA ALFAREI</p><h1>Entre para ver sua loja.</h1><p>Clientes finais veem o preço normal. Revendedores e atacadistas aprovados visualizam as condições do seu grupo e a quantidade mínima de cada produto.</p><a href="{{ route('loja.index') }}#catalogo">← Continuar explorando</a></div><form class="store-account-card" method="POST" action="{{ route('loja.account.login.store') }}">@csrf<h2>Entrar</h2><label>E-mail<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label>@error('email')<small class="account-error">{{ $message }}</small>@enderror<label>Senha<input type="password" name="password" required autocomplete="current-password"></label><button class="store-primary" type="submit">Acessar minha conta →</button><p>Ainda não tem conta? <a href="{{ route('loja.account.register') }}">Cadastre-se como cliente final</a>.</p><small>Quer revender? Crie sua conta e depois acesse <a href="{{ route('loja.reseller.show') }}">Seja revendedor</a>.</small></form></section>
@endsection
